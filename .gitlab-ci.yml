types:
  - deploy

YTP compile and deploy:
  type: deploy
  script:
    - ant clean release
    - NOW=$(date +"%Y-%m-%d_%H-%M-%S")
    - cp "bin/YourTubePlus.apk" "/home/git/gitlab/public/YouTubePlus/YouTubePlus_$NOW.apk"
